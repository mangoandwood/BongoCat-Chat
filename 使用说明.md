# BongoCat 双人版｜简单使用说明

## 1. 第一次使用

1. 两个人都安装并打开同一个版本。
2. 在完整聊天窗口中填写相同的服务器地址和房间名。
3. 双方填写不同昵称，然后连接。
4. 软件会记住这些信息，下次开机自动启动并进入上次房间。

公开版没有默认公共服务器。两人中只需一人按《用户自建服务器-保姆式教程》部署，然后双方填写同一个地址。

## 2. 快捷键

| 快捷键    | 功能                        |
| --------- | --------------------------- |
| `Alt + Q` | 打开或关闭文字输入框        |
| `Alt + 1` | 进入暂离或结束暂离          |
| `Alt + 2` | 隐藏或显示气泡窗口          |
| `Alt + 3` | 按住录音，松开 Alt 自动发送 |

数字使用主键盘上方横排数字，不使用小键盘数字。

## 3. 文字和语音

- 按 `Alt + Q` 后直接打字，按 `Enter` 发送；发送后可继续输入下一条。再按一次 `Alt + Q` 关闭。
- 按住 `Alt + 3` 录音，松开 Alt 自动发送。单条最长 60 秒，第一次使用请允许麦克风权限。
- 对方点击语音气泡即可播放。

## 4. 暂离举牌

1. 按 `Alt + 1` 进入暂离，软件自动发送“我已暂离”。
2. 对方猫咪右上角出现“暂离”牌子。
3. 同时出现一个 5 秒编辑框，默认文字已被选中。
4. 5 秒内直接输入并按 `Enter`，牌子会换成自定义内容，例如“去吃饭”。
5. 不编辑则保持“暂离”。
6. 暂离后再次按键、点击鼠标或移动鼠标，会自动发送“我回来啦”并摘牌。再按 `Alt + 1` 也可手动结束暂离。

## 5. 气泡窗口

- 自己是白色气泡，对方是粉色气泡。
- 左上角小圆点移动窗口，右下角小拉环调整大小。
- 鼠标滚轮可以上下查看消息。
- 设置中可保留 1–30 条气泡。
- `Alt + 2` 隐藏或显示整个气泡窗口。
- 开启“气泡窗口穿透”后鼠标会穿过窗口；需要移动、缩放、滚动或播放语音时请关闭穿透。

## 6. 图片和文件

- 可直接把图片或文件拖进气泡窗口发送。
- 也可在 `Alt + Q` 输入窗口中粘贴或拖入。
- 单个文件最大 5 MB。
- 图片可复制或下载，普通文件显示文件名并可下载。
- 在气泡窗口中单击图片可进入大图预览，单击空白处或右上角关闭。
- 单击普通文件会自动保存到 Windows 桌面，并立即使用系统默认程序打开。
- 如果桌面已有同名文件，软件会自动添加时间后缀，不会覆盖旧文件。
- 右键文字消息可复制内容。

## 7. 声音和边缘提醒

- 对方敲击键盘：播放所选的原创短促啵声，默认音量 22%。
- 对方发来文字、图片、文件或语音：播放所选的原创Q弹提示音，默认音量 35%。
- 两种声音均可在“猫咪设置 → 消息提醒”中分别开关、调整 0–100% 音量并点击试听。
- 软件内置6个由本项目代码合成的原创音效，两处可以选择相同或不同音效。
- 不再播放猫叫。
- 新消息到达时，当前显示器上、下、左、右四条边缘会出现四个独立的亮粉色细条窗口并闪烁两次。
- 四条边缘窗口完全鼠标穿透，不挡住其他软件；不再使用透明全屏提醒层。

公开版不包含来源不明的视频提取音效。内置声音可通过 `scripts/generatePublicSounds.mjs` 重新生成。

## 8. 猫咪和键鼠同步

- 猫咪实时显示对方的键盘按键、鼠标点击和鼠标移动。
- 设置中可选择是否显示对方猫咪。
- 外观、大小和帧率继续使用原有猫咪设置。

## 9. 聊天记录与自动导出

- 文字记录只保存在本机，并按房间分别保存。
- 每轮上限为 2000 条文字消息。
- 达到 2000 条时，软件自动询问是否导出 TXT。
- 确认并成功保存 TXT 后：清空本轮记录，下一轮从 0 条重新计数。
- 取消导出时：不清空，只保留最近 2000 条，之后仍会再次提示。
- 图片、文件和语音只经自建 Cloudflare 节点实时转发，不写入服务器存储；重要文件请及时下载。
- 双方电脑各自保存记录，离线期间没收到的消息不会自动补回。

## 10. 输入次数统计

- 软件统计本机键盘和鼠标输入次数。
- 设置中可显示“今日”“总计”“今日和总计”或“都不显示”。

## 11. 布局预设

1. 先调整猫咪与气泡的位置、大小和穿透状态。
2. 打开“布局预设”，点击“保存当前布局”并命名。
3. 以后选择方案并点击“立即应用”。

预设包括猫咪位置和大小、是否显示对方猫咪、气泡位置和大小、气泡条数、穿透、置顶与帧率。

## 12. 自建服务器、掉线与自动重连

- 本公开版不内置由作者承担额度的公共服务器。每对用户只需一人按照《用户自建服务器-保姆式教程》部署 Cloudflare 节点。
- 另一人只需要复制服务器地址和房间码，不需要 Cloudflare 账号。
- 每 5 分钟发送一次隐藏心跳；网络断开后每 5 秒自动重连。
- Cloudflare 免费额度用尽或平台维护时，节点可能暂时不可用。
- 突然收不到消息时，先查看完整聊天窗口的连接状态并等待自动重连。

## 13. Cloudflare 费用

- 部署时选择 Workers Free，不需要开通 Workers Paid。
- 每个部署者使用自己账户的免费额度，软件作者不代付服务器费用。
- 免费额度存在每日上限，超额时服务会失败，不代表可以无限使用。
- 实际规则以 Cloudflare 后台与官方定价页面为准。

## 14. 常见问题

### 快捷键没有反应

彻底退出旧版，包括右下角托盘中的 BongoCat，再打开新版；确认使用横排数字。

### 对方看不到消息或猫咪不动

确认双方服务器地址、房间名完全相同，并检查连接状态。

### 不能移动气泡或播放语音

关闭“气泡窗口穿透”。

### 安装后仍是旧功能

先退出托盘旧进程再覆盖安装；两个人都需要安装相同版本。

### 安全提醒

不要在公共房间发送密码、证件等隐私文件。房间名过于简单时可能被别人猜到。
