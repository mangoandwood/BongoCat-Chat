# BongoCat 双人聊天版

这是基于 `ayangweb/BongoCat 1.1.0` 制作的基础版本。

## 已实现

- 右键猫咪，选择“打开双人聊天”
- 独立大聊天窗口
- 两人使用相同房间码聊天
- 每个房间最多两人
- 聊天记录保存在各自电脑，最多保留最近 2000 条
- Enter 发送，Shift+Enter 换行
- 可清除当前电脑上的历史记录
- 转发服务器不保存消息

## 本机测试

1. 在 `relay-server` 目录运行 `npm install`，然后运行 `npm start`。
2. 在项目根目录运行 `pnpm install`，然后运行 `pnpm tauri dev`。
3. 两个聊天窗口都填写 `ws://localhost:8787`、相同房间码和不同昵称。

## 互联网双人聊天

将 `relay-server` 部署到支持 WebSocket 的 Node.js 服务平台，启动命令为 `npm start`。平台会提供一个 HTTPS 域名，在客户端中把它写成 `wss://域名`。两台电脑填写相同服务器地址和房间码即可。

房间码相当于临时口令，请使用足够长、难以猜测的随机字符串。这个基础版不含账号系统和端到端加密，不适合传输敏感内容。

## 生成安装包

安装 Node.js、pnpm、Rust 和 Tauri 在 Windows 下需要的构建工具后，在项目根目录运行：

```powershell
pnpm install
pnpm tauri build
```

安装包会生成在 `src-tauri/target/release/bundle/nsis`。
