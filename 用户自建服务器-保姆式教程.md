# BongoCat 用户自建服务器：保姆式教程

## 先说结论

- 每一对聊天用户只需要一个服务器。
- 两个人中只需要一人部署。
- 部署者使用自己的 Cloudflare 免费额度。
- 另一人不需要 Cloudflare 账号，也不需要银行卡，只需安装 BongoCat 并导入服务器地址和房间码。
- 服务器只实时转发，不保存聊天、图片、文件或语音。

## A方：负责部署的人

### 第一步：准备账号

1. 准备一个 GitHub 账号。
2. 打开 <https://dash.cloudflare.com/>，注册或登录 Cloudflare。
3. 如果页面询问套餐，保持 Workers Free，不要开通 Workers Paid。

### 第二步：一键部署

1. 回到本项目 GitHub 首页。
2. 点击 README 顶部的 `Deploy to Cloudflare` 按钮。
3. 登录 Cloudflare并授权读取这个公开仓库。
4. 项目名称保持默认 `bongocat-chat-relay` 即可。
5. 确认部署。Cloudflare 会自动创建 Worker 和 Durable Object。
6. 等待页面显示部署成功。

### 第三步：找到服务器地址

部署完成后会看到类似：

```text
https://bongocat-chat-relay.你的子域名.workers.dev
```

单击这个地址。如果浏览器显示包含以下内容，说明部署成功：

```json
{ "ok": true, "service": "BongoCat Chat Relay" }
```

复制整个地址。软件接受 `https://`，会自动转换成聊天所需的 `wss://`。

### 第四步：在软件里建立房间

1. 打开 BongoCat 的完整聊天窗口。
2. 将刚才的 `https://...workers.dev` 粘贴到“聊天服务器”。
3. 点击“生成安全房间码”。
4. 填写昵称。
5. 点击“测试并进入房间”。
6. 状态显示“已连接”即为成功。
7. 点击“复制邀请信息”，将文字完整发送给B方。

## B方：不负责部署的人

1. 安装并打开 BongoCat。
2. 打开完整聊天窗口。
3. 将A方发来的服务器地址粘贴到“聊天服务器”。
4. 将A方发来的房间码粘贴到“房间码”。
5. 填写自己的昵称。
6. 点击“测试并进入房间”。
7. 显示“已连接”后即可聊天。B方不需要 Cloudflare 账号。

## 下次还要重新部署吗？

不需要。部署一次后服务器地址不会因为关闭软件而改变。双方软件会记住服务器和房间，并在开机启动后自动重连。

## 常见问题

### 显示“无法连接”

1. 在浏览器打开服务器的 `https://` 地址，确认显示 `"ok":true`。
2. 确认双方复制的是同一个服务器地址。
3. 确认双方房间码完全相同，不能多空格。
4. 等待10秒后重新进入房间。

### 显示房间已满

一个房间最多容纳两个不同设备。点击“生成安全房间码”建立新房间。

### 免费额度用完怎么办

等待 Cloudflare 每日额度重置，或让另一位用户按照相同步骤部署另一个节点，再把双方软件改成新地址。

### 是否必须绑定银行卡

普通使用者不需要。部署者应选择 Workers Free。Cloudflare仍有权根据自身风控要求验证账户，本项目无法替第三方平台保证所有注册场景永远不触发身份验证。

### 聊天记录在哪里

聊天记录保存在双方自己的电脑。服务器重启或删除不会删除电脑上的文字记录，但离线期间不会补发消息。
